#include <stdint.h>

int read_sensor(void)
{
    int raw_value;
    int output = 0;

    if (output == 0)
    {
        output = raw_value;
    }

    return output;
}

uint8_t scale_value(uint16_t input)
{
    uint8_t scaled;

    scaled = input;
    return scaled;
}