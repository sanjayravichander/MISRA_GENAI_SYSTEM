# MISRA GenAI System — requirements.txt
# Generated after upgrade to Qdrant + BGE pipeline
#
# Install:
#   venv310\Scripts\activate
#   pip install -r requirements.txt

# ── Core pipeline ──────────────────────────────────────────────
openpyxl==3.1.2              # Excel parsing (parse_polyspace.py, build_excel_kb.py)
numpy>=1.24.0                # vector operations

# ── Embeddings ─────────────────────────────────────────────────
sentence-transformers==3.0.1  # BGE + MiniLM models
# Model downloaded on first use:
#   BAAI/bge-base-en-v1.5  (~440MB, to ~/.cache/huggingface)
#   all-MiniLM-L6-v2       (~22MB, legacy — kept for fallback)

# ── Vector store ───────────────────────────────────────────────
qdrant-client>=1.9.0         # Qdrant local on-disk (no server needed)
#faiss-cpu==1.8.0             # FAISS — kept for legacy fallback only

# ── PDF processing (KB build, run once) ────────────────────────
pypdf==4.3.1

# ── Reporting ──────────────────────────────────────────────────
reportlab==4.2.2
python-docx==1.1.2

# ── Web UI ─────────────────────────────────────────────────────
flask==3.0.3
flask-cors==4.0.1

# ── Dev / optional ─────────────────────────────────────────────
# rank-bm25>=0.2.2           # BM25 lexical retrieval (Priority 3, optional)